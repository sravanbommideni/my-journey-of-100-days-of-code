if start == NULL :
  if step == +ve : considers start as starting index
  if step == -ve : considers start as last index

if stop == NULL :
  if step == +ve : considers start as last index
  if step == -ve : considers start as starting index
